ExUnit.start

# Want to set up test dependencies and it's a pain to
# remember mix test --no-start so we'll stop it here
#Elevator.stop
# no need any more since I made logging configurable with config.exs
